# Codex 用量悬浮球

适用于 Windows 10/11 x64 的 Codex 用量悬浮球。它只专注一件事：让你随时看到 Codex 剩余用量，同时尽量不打扰工作。

## 开始使用

1. 确保 Codex 桌面应用已经安装并登录。
2. 双击 `Codex 用量悬浮球.exe`，首次读取用量通常需要几秒钟。
3. 第一次运行会登记 Windows 登录启动项；以后登录 Windows 后会在后台静默等待 Codex。

程序是自包含单文件，不需要安装 .NET，也不会在桌面自动创建快捷方式。

## 操作方式

- 悬停球体：查看 Codex 当前实际返回的全部额度、已用比例、剩余比例和重置时间。
- 双击球体：打开 Codex。
- 拖动球体：自由移动；靠近屏幕边缘时自动吸附。
- 右键球体：刷新额度、选择球体显示的额度、打开设置或退出程序。
- 托盘菜单：显示悬浮球、打开设置或退出程序。

## 用量显示

- 球体中心只显示当前选中额度的剩余百分比。
- 默认使用“智能推荐”：全部额度正常时优先显示最短周期；出现预警时优先显示风险更高、剩余更少的额度。
- 官方增加或移除额度窗口时，悬停卡片和球体选择会自动跟随，不保留固定占位。
- 外圈颜色会随用量从薄荷绿连续过渡到黄色、红色。

## 设置

- 球体大小：48–96 px。
- 球体透明度：30%–100%。
- 主题：跟随系统、浅色或深色。
- 黄色提醒和红色警告阈值。
- 设置窗口大小。
- 始终置顶。
- 随 Codex 自动显示。
- Codex 关闭后隐藏：关闭约 30 秒后隐藏并停止读取。
- 单击切换额度：默认关闭。
- 界面动画：低性能设备可关闭。
- 恢复全部默认。

## 刷新与资源占用

- 悬浮球可见时，额度数据最长约 60 秒自动刷新一次。
- 悬停时，如果数据超过 30 秒未更新，会优先刷新。
- 隐藏后会断开本地用量读取进程。
- 静止时没有持续呼吸、光晕或扫光动画。

## 数据与隐私

程序只通过本机 Codex App Server 的 `account/rateLimits/read` 读取额度窗口，不抓取网页，不读取或保存 ChatGPT 密码，也不会把用量上传到第三方服务。

## Windows SmartScreen

这是未使用商业代码签名证书的个人构建。请核对同目录的 `SHA256.txt`；确认文件来源后，可在 SmartScreen 中选择“更多信息”并运行。

## 卸载

1. 在设置中关闭“随 Codex 自动显示”，移除 Windows 登录启动项。
2. 右键悬浮球或托盘图标，选择“退出”。
3. 删除程序文件。设置保存在 `%LOCALAPPDATA%\CodexUsageBall`，需要时可自行删除。

程序不会安装 Windows 系统服务或驱动。

## 版本

1.8.8 · 2026-08-27
